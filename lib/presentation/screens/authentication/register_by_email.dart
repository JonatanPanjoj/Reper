import 'package:flutter/material.dart';

class RegisterByEmailScreen extends StatelessWidget {
  const RegisterByEmailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold( body: Center(child: Text('RegisterS'),),);
  }
}